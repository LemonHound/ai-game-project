[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = src/backend
markers =
    serial: marks tests that depend on cache state and must run sequentially
